jQuery UI Components
Widgets: Autocomplete, Datepicker, Slider

Use the Smoothness theme since it is visually compatible with most other designs. This is important since EWF apps could be using the default EWF UI theme, a custom theme, or not using the EWF UI at all.